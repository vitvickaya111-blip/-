# PDF гайд

## Текущая конфигурация:

PDF-гайд "Вьетнам с ребёнком за 70 000₽" загружается напрямую по ссылке:

**URL:** https://pdfhost.io/v/Sf7YZvWQga_vietnam

Бот автоматически отправляет PDF файл при:
- Нажатии на кнопку "📥 Скачать PDF-гайд" в результатах квиза
- Нажатии на кнопку "Получить гайд по Вьетнаму"

## Как обновить PDF:

1. Загрузите новый PDF на https://pdfhost.io
2. Скопируйте новую ссылку
3. Обновите URL в файле `backend/bot/handlers/user/start.py`:
   - В обработчике `download_guide_handler` (строка ~236)
   - В обработчике `get_guide_handler` (строка ~329)
4. Пересоберите Docker контейнер:
   ```bash
   docker compose -f docker/test.yml --env-file docker/.env.test up -d --build bot
   ```
